<div class="home" id="inicio">
	<video playsinline autoplay muted loop id="bgVideo" poster="/home.webp">
		<source src="/home.mp4" type="video/mp4" />
	</video>
  <div class="title">
    <span class="line" />
    <h1>Live marketing brand activation company since 2009</h1>
    <span class="line" />
  </div>
  <div class="cta">
    <a class="link main-link" href="https://wa.me/573174318887" target="_blank" rel="noopener noreferrer" aria-label="Whatsapp">Contáctanos</a>
    <a href="#servicios" class="link">Conócenos</a>
  </div>				
</div>

<style>
  .home {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		height: 100vh;
		color: var(--foreground);
		margin-top: -78px;
		row-gap: 2rem;
	}
	#bgVideo {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 100%;
		height: 100%;
		object-fit: cover;
		z-index: -100;
		transform: translateX(-50%) translateY(-50%);
		filter: brightness(0.3);
	}
	.title {
		display: flex;
		text-align: center;
		max-width: 900px;
		column-gap: 1rem;
	}
	.line {
		width: 200px;
		height: 1px;
		background-color: var(--primary);
		margin-top: 1.75rem;
	}
	.title h1 {
		font-weight: 400;
		font-size: 3rem;
		margin: 0;
	}
	.cta {
		display: flex;
		gap: 1rem;
	}
	.link {
		color: var(--foreground);
		text-decoration: none;
		padding: 0.75rem 2rem;
		border-radius: 5px;
		background-color: var(--secondary);
	}
	.link:hover {
		background-color: var(--secondary-light);
	}
	.main-link {
		background-color: var(--primary);
	}
	.main-link:hover {
		background-color: var(--primary-light);
	}
	
	@media (max-width: 1024px) {
		.title {
			max-width: 500px;
		}

		.line {
			width: 150px;
		}

		.title h1 {
			font-size: 2rem;
		}
  }

  @media (max-width: 768px) {
		.home {
			margin-top: 58px;
		}
		.title {
			max-width: 500px;
		}

		.line {
			width: 60px;
		}

		.title h1 {
			font-size: 1.5rem;
			max-width: 300px;
		}
	}
</style>
